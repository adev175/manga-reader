# Python
__pycache__/
*.pyc
.venv/

# Ảnh manga (quá lớn cho git thường — dùng LFS hoặc bỏ vào .gitignore nếu self-host)
# Bỏ comment dòng dưới nếu KHÔNG muốn commit ảnh lên GitHub:
# docs/chapters/

# Custom ignores
downloads/
scratch/
cookies.json

